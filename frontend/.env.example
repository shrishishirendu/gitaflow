# The frontend has no secrets — all Anthropic calls go through the backend.
# Override the API base URL only if you're hosting the backend somewhere
# other than localhost:8000 (the Vite proxy handles dev automatically).

# VITE_API_BASE=http://localhost:8000
